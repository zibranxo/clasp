# Worker Milestone 3 Task Request

## Mandatory Integrity Warning
DO NOT CHEAT. All implementations must be genuine. DO NOT hardcode test results, create dummy/facade implementations, or circumvent the intended task. A Forensic Auditor will independently verify your work. Integrity violations WILL be detected and your work WILL be rejected.

## Objective
Implement Milestone 3: OpenAI Responses API & Codex Support.

Specifically:
1. Port all files from `free-claude-code-main/core/openai_responses/` to `clasp/core/openai_responses/`. Make sure all relative imports and references are correctly updated to `clasp.core.openai_responses`.
2. Implement a new route `POST /v1/responses` in `clasp/api/proxy_routes.py`.
   - The route must require bearer token verification (`Depends(_verify_bearer)`).
   - Ensure the request body has `stream = True` (if `stream = False` or omitted, return 400 Bad Request with appropriate error payload).
   - Use the ported `OpenAIResponsesAdapter` class to convert the OpenAI Responses request payload to an Anthropic Messages request payload via `to_anthropic_payload()`.
   - Delegate the translated payload to `handle_request` in `clasp/api/service.py` to stream the request from the chosen provider.
   - Stream the result back to the client using `StreamingResponse`, yielding the translated SSE events generated by `iter_sse_from_anthropic()`.
3. Generate a local Codex model catalog file at `~/.fcc/codex-model-catalog.json` (or use a settings path if configurable, or default to a standard user directory path) containing the model names. Let's look at how FCC generates it in `free-claude-code-main/cli/launchers/codex.py` or similar files, and make sure CLASP can generate/save it on server startup or model list refresh.
4. Verify the implementation by writing a unit test suite under `tests/unit/test_responses.py` that mocks the responses endpoint and validates the translation, streaming, tool-calling mapping, and model catalog generation. Run all tests to verify.


## 2026-06-23T15:56:35Z
You are a worker tasked with executing the Milestone 3 requirements in `c:\code\clasp\.agents\worker_m3\ORIGINAL_REQUEST.md`.
Please read that file, implement the requirements, and verify the changes by running unit and integration tests.
Ensure you write/maintain a `progress.md` file in `c:\code\clasp\.agents\worker_m3\progress.md` with your status, last visited timestamp, and current step.
Once done, write `handoff.md` and message the parent agent (c3d26509-90be-4ebf-9f0c-824b9c1d8712) with your final status and verification results.
